<div class="main-visual">
  <div class="content">
    <h1>トップページ</h1>
    <div class="content-comment">新規投稿からコメントを投稿しよう</div>
    <% @posts.each do |post| %>
      <div class="content-inner">
        <div class="content-date">
          投稿日時：<%= post.created_at.strftime("%Y年 %m月 %d日 %H時 %M分") %>
        </div>
        <div class="content-text">
          <%= post.content %>
        </div>
      </div>
    <% end %>

  </div>
</div>